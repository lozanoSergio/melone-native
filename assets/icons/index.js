import {ImageIcon} from './ImageIcon';

export const StarIconOutline = style => {
  const source = {
    imageSource: require('./eva/star-outline.png'),
  };
  return ImageIcon(source, style);
};

export const MessageCircleIconOutline = style => {
  const source = {
    imageSource: require('./eva/message-circle-outline.png'),
  };
  return ImageIcon(source, style);
};

export const HomeIconOutline = style => {
  const source = {
    imageSource: require('./eva/home-outline.png'),
  };
  return ImageIcon(source, style);
};

export const CubeIconOutline = style => {
  const source = {
    imageSource: require('./eva/cube-outline.png'),
  };
  return ImageIcon(source, style);
};

export const ClockIconOutline = style => {
  const source = {
    imageSource: require('./eva/clock-outline.png'),
  };
  return ImageIcon(source, style);
};

export const PersonIconOutline = style => {
  const source = {
    imageSource: require('./eva/person-outline.png'),
  };
  return ImageIcon(source, style);
};

export const ArrowBackIconOutline = style => {
  const source = {
    imageSource: require('./eva/arrow-back-outline.png'),
  };
  return ImageIcon(source, style);
};

export const CameraIconFill = style => {
  const source = {
    imageSource: require('./eva/camera.png'),
  };
  return ImageIcon(source, style);
};

export const CloseIconOutline = style => {
  const source = {
    imageSource: require('./eva/close-outline.png'),
  };
  return ImageIcon(source, style);
};

export const HeartIconFill = style => {
  const source = {
    imageSource: require('./eva/heart.png'),
  };
  return ImageIcon(source, style);
};

export const HeartIconOutline = style => {
  const source = {
    imageSource: require('./eva/heart-outline.png'),
  };
  return ImageIcon(source, style);
};

export const BulbIconFill = style => {
  const source = {
    imageSource: require('./eva/bulb.png'),
  };

  return ImageIcon(source, style);
};

export const MoreHorizontalIconFill = style => {
  const source = {
    imageSource: require('./eva/more-horizontal.png'),
  };

  return ImageIcon(source, style);
};

export const Trash2IconOutline = style => {
  const source = {
    imageSource: require('./eva/trash-2-outline.png'),
  };

  return ImageIcon(source, style);
};

export const ButterIcon = style => {
  const source = {
    imageSource: require('./flat/butter.png'),
  };

  return ImageIcon(source, style);
};

export const SaltIcon = style => {
  const source = {
    imageSource: require('./flat/salt.png'),
  };

  return ImageIcon(source, style);
};

export const SugarIcon = style => {
  const source = {
    imageSource: require('./flat/sugar.png'),
  };

  return ImageIcon(source, style);
};

export const CaloriesIcon = style => {
  const source = {
    imageSource: require('./flat/calories.png'),
  };

  return ImageIcon(source, style);
};

export const AllergenIcon = style => {
  const source = {
    imageSource: require('./flat/allergen.png'),
  };

  return ImageIcon(source, style);
};

export const FabricIcon = style => {
  const source = {
    imageSource: require('./flat/fabric.png'),
  };

  return ImageIcon(source, style);
};

export const AdditivesIcon = style => {
  const source = {
    imageSource: require('./flat/molecule.png'),
  };

  return ImageIcon(source, style);
};

export const FacebookIcon = style => {
  const source = {
    imageSource: require('./eva/facebook.png'),
  };

  return ImageIcon(source, style);
};

export const GoogleIcon = style => {
  const source = {
    imageSource: require('./flat/g-logo.png'),
  };

  return ImageIcon(source, style);
};
